@using Data.EF.Entities;

@model IEnumerable<Field>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

@if (Model.Count() > 0)
{
    foreach(var field in Model)
    {
        <p>@field.Id</p>
    }
}